[tool.pytest.ini_options]
testpaths = ["backend/tests"]
pythonpath = ["."]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::DeprecationWarning:crewai.*",
    "ignore::PendingDeprecationWarning:crewai.*",
]
